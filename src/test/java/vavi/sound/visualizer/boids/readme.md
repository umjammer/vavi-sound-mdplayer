# boids visualizer

one flock per channel, crossing over, swelling on each note

* every number is a slider (`BoidsControlPanel`), presets are only a starting point
* see it with `BoidsVisualizerDemo` (synthetic) or `BoidsPlayTest` (a real song)
* not in the player's own window yet, same as fmdsp
